import os

extension Logger {
    static let audioCD = Logger(subsystem: "org.burnman.BurnMan", category: "AudioCD")
    static let devices = Logger(subsystem: "org.burnman.BurnMan", category: "Devices")
    static let helper  = Logger(subsystem: "org.burnman.BurnMan", category: "Helper")
}
